import 'package:flutter/material.dart';

class PlayersAddScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Add Player')),
      body: Center(child: Text('Add a New Player')),
    );
  }
}
