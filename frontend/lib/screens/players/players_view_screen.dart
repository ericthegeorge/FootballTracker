import 'package:flutter/material.dart';

class PlayersViewScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('View Players')),
      body: Center(child: Text('View Players')),
    );
  }
}
