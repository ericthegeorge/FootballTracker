import 'package:flutter/material.dart';

class MatchesAddScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Add Match')),
      body: Center(child: Text('Add a New Match')),
    );
  }
}
