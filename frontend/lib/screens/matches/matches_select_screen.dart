import 'package:flutter/material.dart';

class MatchesSelectScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Select Match')),
      body: Center(child: Text('Select a Match')),
    );
  }
}
