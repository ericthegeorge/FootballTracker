import 'package:flutter/material.dart';

class MatchesViewScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('View Matches')),
      body: Center(child: Text('View Matches')),
    );
  }
}
