import 'package:flutter/material.dart';

class MatchesSearchScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Search Matches')),
      body: Center(child: Text('Search Matches')),
    );
  }
}
