import 'package:flutter/material.dart';

class LeaguesSelectScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Select League')),
      body: Center(child: Text('Select a League')),
    );
  }
}
