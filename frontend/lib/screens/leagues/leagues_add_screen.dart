import 'package:flutter/material.dart';

class LeaguesAddScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Add League')),
      body: Center(child: Text('Add a New League')),
    );
  }
}
