import 'package:flutter/material.dart';

class LeaguesViewScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('View Leagues')),
      body: Center(child: Text('View Leagues')),
    );
  }
}
