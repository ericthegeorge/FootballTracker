import 'package:flutter/material.dart';

class LeaguesSearchScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Search Leagues')),
      body: Center(child: Text('Search Leagues')),
    );
  }
}
