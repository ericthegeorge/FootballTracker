import 'package:flutter/material.dart';

class TeamsSelectScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Select Team')),
      body: Center(child: Text('Select a Team')),
    );
  }
}
