import 'package:flutter/material.dart';

class TeamsSearchScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Search Teams')),
      body: Center(child: Text('Search Teams')),
    );
  }
}
