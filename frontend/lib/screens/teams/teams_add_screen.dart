import 'package:flutter/material.dart';

class TeamsAddScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Add Team')),
      body: Center(child: Text('Add a New Team')),
    );
  }
}
