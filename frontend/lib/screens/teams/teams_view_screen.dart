import 'package:flutter/material.dart';

class TeamsViewScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('View Teams')),
      body: Center(child: Text('View Teams')),
    );
  }
}
